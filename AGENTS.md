# Project Instructions

## Modularity

- Prefer modular code: split features and providers into focused files instead of growing large mixed-responsibility modules.
- Each task provider should live in its own module. Keep registry/barrel files thin.
